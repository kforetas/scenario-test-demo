apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-demo-efs-pvc1
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: efs-csi
  volumeMode: Filesystem
